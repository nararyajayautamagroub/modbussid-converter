# frontend

Website responsive Game Mod Asset Lab.

File utama: `index.html`, `app.js`, `styles.css`, `manifest.webmanifest`, `sw.js`, `icon.svg`.

Semua aksi UI memakai JavaScript event delegation. Halaman utama tidak memakai inline `onclick` atau inline CSS.
