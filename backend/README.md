# backend

FastAPI backend dan gateway aplikasi.

Berisi application entrypoint, authentication, repository catalog, scraper API, asset API, template API, sprite API, i18n API, dan gateway API.

Entrypoint: `backend/app.py`

Jalankan dengan `npm start`.
